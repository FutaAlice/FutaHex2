PROJECT (FHUTILS)

FILE(GLOB SOURCE_FILES "./*.cpp")
INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/include)
INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/include/fhutils)

ADD_LIBRARY(fhutils STATIC ${SOURCE_FILES})
